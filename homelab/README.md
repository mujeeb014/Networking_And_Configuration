# Home Lab

Self-built vulnerable environments and full attack-chain documentation — going beyond pre-made CTF boxes to design and attack my own small networks (e.g., Metasploitable + DVWA on a virtual network).

Each subfolder should include:
- A network topology diagram
- The full attack narrative (recon → exploitation → privesc)
- What I'd change if defending this same setup

Folder naming convention: `YYYY-MM_attack-chain-name/`
